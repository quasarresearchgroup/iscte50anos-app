name: iscte_spots
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  qr_code_scanner: ^0.6.1
  qr_flutter: ^4.0.0
  http: ^0.13.4
  html: ^0.15.0
  synchronized: ^3.0.0
  flutter_localizations:
    sdk: flutter      
  intl: ^0.17.0    
  logger: ^1.1.0
  timeline_tile: ^2.0.0
  sqflite: ^2.0.0+4
  path_provider: ^2.0.7
  country_icons: ^2.0.2
  flutter_svg: ^1.0.0
  fluttericon: ^2.0.0
  image: ^3.1.1
  animated_splash_screen: ^1.2.0
  motion_sensors: ^0.1.0
  

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  url_launcher: ^6.0.12

flutter:
  generate: true
  uses-material-design: true
  assets:
    - Resources/
    - Resources/Img/
    - Resources/Img/Logo/
    - Resources/NewQrCodes/alternatives/